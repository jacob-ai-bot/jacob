export enum TaskType {
  task = "task",
  code = "code",
  design = "design",
  terminal = "terminal",
  plan = "plan",
  prompt = "prompt",
  issue = "issue",
  pull_request = "pull request",
  command = "command",
}
